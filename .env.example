# MongoDB connection URI
MONGODB_URI=mongodb://localhost:27017/zynentia

# JWT secret key – MUST be changed to a long random string before deploying to production
JWT_SECRET=your-super-secret-jwt-key-here

# Server port (optional, defaults to 3000)
PORT=3000
